const databaseConfig = {
    user: 'www',
    database: 'www',
    password: 'testpassword',
    port: 5432
};

const siteName = 'testsite.com';

module.exports = {
   databaseConfig: databaseConfig,
   siteName: siteName
}
